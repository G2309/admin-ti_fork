# Copiar a .env y ajustar:
#   cp .env.example .env
#
# El .env real esta en .gitignore. Solo afecta a los scripts de scripts/*.sh.

# Puerto del host donde se publica el preview (http://localhost:PREVIEW_PORT).
# Cambialo si 8080 esta ocupado por otro servicio.
PREVIEW_PORT=8080
